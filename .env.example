# Copy this file to .env.local to send demo events to Meiro Pipes.
# Leave the endpoint unset to keep the storefront in local-preview mode.
# NEXT_PUBLIC_* values are public and fixed when Next.js builds the client.

NEXT_PUBLIC_MEIRO_COLLECTION_ENDPOINT=https://your-pipes-domain/collect/web-sdk
